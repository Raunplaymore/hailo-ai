# Example training configuration
experiment_name: demo_run
seed: 42

paths:
  data_dir: ../data
  output_dir: ../outputs

training:
  epochs: 5
  batch_size: 32
  learning_rate: 0.001

model:
  name: simple_model
  hidden_dim: 128
